node_modules/
dist/
dist-node/
.env
.env.*
*.local
.DS_Store
Thumbs.db

# локальные store, собранные для импорта или экспериментов
store.json
store-*.json
# страховочная копия store прежнего формата (кладётся рядом при первой записи после миграции)
*.v3.bak

# рабочие артефакты агентских процессов: черновики, отчёты, diff-пакеты ревью
.superpowers/
